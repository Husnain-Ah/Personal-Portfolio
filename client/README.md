# Client (React)

This folder contains a minimal Vite + React app for local development.

Commands:

- Install dependencies: `cd client` then `npm install`
- Run dev server: `npm run dev`
- Build for production: `npm run build`
